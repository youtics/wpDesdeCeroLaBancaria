import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'wp-desde-cero',
  webDir: 'dist/wp-desde-cero',
  bundledWebRuntime: false
};

export default config;
